<?xml version="1.0" encoding="utf-8"?>
<!--
  updatePeriodMillis is 0. There is nothing to poll: the state only changes when
  Bluetooth changes, and BudsStateReceiver is told that by the system. A period
  here would wake the phone on a timer to redraw something that had not moved.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:minResizeWidth="110dp"
    android:minResizeHeight="80dp"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/widget_buds"
    android:description="@string/buds_widget_description" />
