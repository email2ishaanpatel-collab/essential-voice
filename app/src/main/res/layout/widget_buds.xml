<?xml version="1.0" encoding="utf-8"?>
<!--
  The buds card, on the home screen.

  One static layout for both states: the fill, the glyph tint and the two text
  colours carry connected/disconnected between them. There is no animated view
  here and there is no ViewFlipper — RemoteViews will only call methods marked
  @RemotableViewMethod, and ViewFlipper.setAutoStart is not one of them, so
  driving it throws at apply() time and the launcher shows "Can't load widget".

  ImageView rather than View for the glyphs: a RemoteViews layout may only
  contain classes on the framework's allow-list, and a bare View is not one.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/buds_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/widget_surface"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingBottom="10dp">

        <!-- The status dot. Its pixels are pushed as a Bitmap rather than set
             from a drawable, because it blinks while connecting and a widget
             cannot animate a drawable of its own — see BudsGlyphAnim. -->
        <ImageView
            android:id="@+id/buds_glyph"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:contentDescription="@null" />
    </LinearLayout>

    <TextView
        android:id="@+id/buds_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/widget_ink"
        android:textSize="15sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="@string/buds_widget_label" />

    <TextView
        android:id="@+id/buds_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="@color/widget_ink_muted"
        android:textSize="13sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="@string/buds_tap_to_connect" />
</LinearLayout>
